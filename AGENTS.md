# go-stakeholder AGENTS

1. Use Go modules.
2. Commands:
   - `go build ./...`
   - `go test ./...`
3. Standard library-first.
4. Wire `core/` as submodule and parse shared fixtures.
5. Parity-first architecture with deterministic output (`--seed`, `--output-format`).
